Review the change in the current working directory. Run `git diff`, `git diff --cached` and `git status` and read the result before reporting anything. The worker's own account of what it did follows — treat it as a claim to check, not as the change itself.

If those commands show no change at all, report `Nothing to review.` and stop. An empty tree is a result, and it is not the same result as a change with nothing wrong in it. Do not review the account instead.

<<SUMMARY>>