First run `git diff $1` and read the diff. We are in a git worktree setting, and running `git diff` will just work.

Give a comprehensive code review based on that diff output. We only need to consider code that is different between the current commit and $1, do not include outside considerations in your review.

Additional focus: $2
