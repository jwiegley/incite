Three workers each implemented the plan independently, in their **own git
worktree**. Their summaries and worktree paths are below.

Go and read the three worktrees — the summaries are a map, not the territory,
and a worker that oversold its work will read differently in the diff.

Then pick the best implementation, or synthesise across them where one worker
got a part right that the others did not, and apply the result in the **CURRENT**
repository by editing the files directly. Do not describe the change; make it.

Finish with: which worker you took as the base, what you grafted in from the
others, and what you rejected and why.
